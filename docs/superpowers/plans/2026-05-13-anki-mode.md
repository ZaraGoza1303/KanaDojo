# Anki Mode Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Tambah mode Anki flashcard SRS penuh (flip + Again/Hard/Good/Easy) dengan 300 kosakata N5-N4 tanpa limit harian.

**Architecture:** Data `vocab.js` → engine `srs.js` (SM-2, localStorage) → view `AnkiMode.jsx` (queue due+new, flip, grade, XP) → wiring `App.jsx`/`Home.jsx`. Romaji via `kanaTextToRomaji`, progress via `useProgress`.

**Tech Stack:** React 18, Vite, Tailwind, localStorage, SM-2

**Spec:** `docs/superpowers/specs/2026-05-13-anki-mode-design.md`

## Global Constraints
- Kana murni hiragana/katakana + spasi, tanpa kanji — verbatim dari spec
- localStorage key `kana-dojo-anki-v1`
- No daily limit — semua new cards available
- SM-2: ease0 2.5, floor 1.3, Again 1m, Hard 10m, Good 1d, Easy 4d

---

### Task 1: Vocab Data 300 entries

**Files:**
- Create: `src/data/vocab.js`
- Test: manual via node -e import

**Interfaces:**
- Consumes: none
- Produces: `export const VOCAB = [{id,kana,arti,contoh_kana,contoh_arti,kategori,level}]` — 300 entries, id 1-300, level 1/2, kategori string

- [ ] **Step 1: Create vocab.js with 300 entries**
```js
// src/data/vocab.js
export const VOCAB = [
  { id: 1, kana: 'ねこ', arti: 'kucing', contoh_kana: 'ねこが すきです', contoh_arti: 'Saya suka kucing', kategori: 'Nomina', level: 1 },
  // ... 299 more, semua N5-N4, variatif tema, tanpa kanji
]
```

- [ ] **Step 2: Verify no kanji & count**
```bash
node -e "import('./src/data/vocab.js').then(m=>{console.log(m.VOCAB.length); const k=/[\u4e00-\u9faf]/; console.log(m.VOCAB.filter(x=>k.test(x.kana)).length)})"
```
Expected: 300, 0

- [ ] **Step 3: Verify romaji generation**
```bash
node -e "import('./src/data/vocab.js').then(v=>import('./src/lib/romaji.js').then(r=>{console.log(r.kanaTextToRomaji(v.VOCAB[0].kana))}))"
```
Expected: romaji string e.g. "neko"

- [ ] **Step 4: Commit**
```bash
git add src/data/vocab.js
git commit -m "feat: add vocab data 300 entries N5-N4 for Anki"
```

---

### Task 2: SRS Engine

**Files:**
- Create: `src/lib/srs.js`
- Test: `node -e` manual checks (no test framework in repo, use node one-liners)

**Interfaces:**
- Consumes: `VOCAB` ids, `Date.now()`
- Produces:
  - `loadSRS() => Map<id, {ease, intervalMin, due, reps, lapses}>`
  - `saveSRS(map)`
  - `schedule(state, grade)=> newState`
  - `gradeCard(id, grade)` — grade 1=Again,2=Hard,3=Good,4=Easy
  - `getQueue(vocab, srsMap, now) => {due:[], newCards:[], upcoming:[]}`
  - `STORAGE_KEY = 'kana-dojo-anki-v1'`

- [ ] **Step 1: Write srs.js skeleton with SM-2 logic**
```js
export const STORAGE_KEY='kana-dojo-anki-v1'
export function loadSRS(){ try{const j=localStorage.getItem(STORAGE_KEY); return new Map(j?JSON.parse(j):[])}catch{return new Map()}}
export function saveSRS(map){ localStorage.setItem(STORAGE_KEY, JSON.stringify([...map])) }
export function schedule(state, grade){
  let {ease=2.5, intervalMin=0, reps=0, lapses=0}=state||{}
  if(grade===1){ ease=Math.max(1.3,ease-0.2); intervalMin=1; lapses++ }
  else if(grade===2){ intervalMin=Math.max(10, Math.round((intervalMin||10)*1.2)) }
  else if(grade===3){ reps++; intervalMin=reps===1?1440:Math.round(intervalMin*ease) }
  else if(grade===4){ reps++; intervalMin=reps===1?5760:Math.round(intervalMin*ease*1.3); ease+=0.15 }
  const due=Date.now()+intervalMin*60000
  return {ease, intervalMin, due, reps, lapses}
}
export function getQueue(vocab, srsMap, now=Date.now()){
  const due=[], newCards=[], upcoming=[]
  for(const v of vocab){ const s=srsMap.get(v.id); if(!s) newCards.push(v); else if(s.due<=now) due.push({...v,_s:s}); else upcoming.push({...v,_s:s})}
  due.sort((a,b)=>a._s.due-b._s.due)
  return {due, newCards, upcoming}
}
export function gradeCard(id, grade){
  const m=loadSRS(); const cur=m.get(id)||{}; const next=schedule(cur, grade); m.set(id,next); saveSRS(m); return next
}
```

- [ ] **Step 2: Run manual test**
```bash
node -e "
import('./src/lib/srs.js').then(s=>{
  let st=null; st=s.schedule(st,1); console.log('Again',st.intervalMin===1);
  st=s.schedule({ease:2.5,intervalMin:1440,reps:1},3); console.log('Good',st.intervalMin>1000);
  console.log('load empty', s.loadSRS() instanceof Map);
})
"
```
Expected: true true true

- [ ] **Step 3: Test queue logic**
```bash
node -e "
import('./src/lib/srs.js').then(async s=>{
  const v=[{id:1},{id:2},{id:3}]; const m=new Map([[1,{due:Date.now()-1000}],[3,{due:Date.now()+999999}]]);
  const q=s.getQueue(v,m); console.log(q.due.length===1 && q.newCards.length===1 && q.upcoming.length===1)
})
"
```
Expected: true

- [ ] **Step 4: Commit**
```bash
git add src/lib/srs.js
git commit -m "feat: add SRS engine SM-2 with localStorage"
```

---

### Task 3: AnkiMode View

**Files:**
- Create: `src/views/AnkiMode.jsx`
- Modify: `src/components/ui.jsx` (if need, reuse existing)
- Test: `npm run build`

**Interfaces:**
- Consumes: `VOCAB` from data/vocab.js, `loadSRS, saveSRS, schedule, getQueue, gradeCard` from lib/srs.js, `kanaTextToRomaji` from lib/romaji.js, `progress` prop
- Produces: `export default function AnkiMode({progress,onExit})`

- [ ] **Step 1: Create AnkiMode.jsx**
```jsx
import {useState,useMemo} from 'react'
import {VOCAB} from '../data/vocab.js'
import {loadSRS,saveSRS,schedule,getQueue} from '../lib/srs.js'
import {kanaTextToRomaji} from '../lib/romaji.js'
// state: srsMap, current, flipped, history stack, session stats
// queue = getQueue(VOCAB, srsMap)
// current = queue.due[0] || queue.newCards[0] || queue.upcoming[0]
// flip toggles, grade updates srsMap via schedule, save, push history for undo, addXp
// render: Card depan kana besar, belakang arti+romaji+contoh, 4 tombol grade, header stats, undo/reset/beranda
```

- [ ] **Step 2: Build check**
```bash
npm run build
```
Expected: built without error

- [ ] **Step 3: Commit**
```bash
git add src/views/AnkiMode.jsx
git commit -m "feat: add AnkiMode flip + SRS grading"
```

---

### Task 4: Wiring App & Home

**Files:**
- Modify: `src/App.jsx:1-12,68-82` — import AnkiMode, add view 'anki'
- Modify: `src/views/Home.jsx:7-12` — add MODES entry for anki

**Interfaces:**
- Consumes: `AnkiMode` component
- Produces: navigation to anki from Home, back via onExit

- [ ] **Step 1: Edit App.jsx**
```js
import AnkiMode from './views/AnkiMode.jsx'
// add {view==='anki' && <AnkiMode progress={progress} onExit={goHome} />}
```

- [ ] **Step 2: Edit Home.jsx**
```js
{ id:'anki', title:'Mode Anki', desc:'Flashcard kosakata 300 kata N5-N4, flip & SRS', tags:['300 kata','SRS','Flip'] }
```

- [ ] **Step 3: Build & manual navigate test**
```bash
npm run build && echo ok
```
Expected: ok

- [ ] **Step 4: Commit**
```bash
git add src/App.jsx src/views/Home.jsx
git commit -m "feat: wire Anki mode into navigation"
```

---

### Task 5: Final Verification

**Files:**
- Test: build + node checks + browser manual

- [ ] **Step 1: Full build**
```bash
npm run build
```
Expected: ✓ built

- [ ] **Step 2: Kanji check for vocab**
```bash
node -e "import('./src/data/vocab.js').then(m=>{const k=/[\u4e00-\u9faf]/; console.log('bad',m.VOCAB.filter(x=>k.test(x.kana)||k.test(x.contoh_kana)).length)})"
```
Expected: bad 0

- [ ] **Step 3: SRS persistence smoke**
```bash
node -e "import('./src/lib/srs.js').then(s=>{let a=s.schedule(null,3); console.log(a.due>Date.now())})"
```
Expected: true

- [ ] **Step 4: Commit (if fixes)**
```bash
git status
```
